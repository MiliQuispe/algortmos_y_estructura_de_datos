#include "alumnos.h"

int main()
{
    altasYbajasDeLosAlumnos("alumnos.dat");
//
////    tArbol pa;
////
////    crearLote();
////
////    crearArbol(&pa);
////    cargarArbol_archBin("alumnos.dat", &pa, sizeof(tAlumnos), extraeDni);
////
////    mapInOrden(&pa, NULL,mostrarIndice);
////
////
////    cargarArchivoIDX_arbol("alumnos.idx",&pa, escribirArchivoAlumnosIdx);
////    mapInOrden(&pa, NULL,mostrarIndice);
////
////    vaciarArbol(&pa);
////    crearArbol(&pa);
////    cargarArbol_archIdx("alumnos.idx",&pa, sizeof(tDni),leerArchivoIdx);
////    mapInOrden(&pa, NULL,mostrarIndice);
////
////    bajaAlumnos("alumnos.dat",&pa);
////
////    vaciarArbol(&pa);

    return 0;
}
